### Feature branch: clips files/grid/search + AI streaming selection
### Captured: 2026-06-17 ~06:00 EDT

BUILD (release): `swift build -c release` -> Build complete!
BUILD (debug):   `swift build -c debug`   -> Build complete!

TESTS: `swift test` (Xcode-beta toolchain, scratch path outside iCloud)
  - New ClipSearchQueryTests: 12 passed
      * parser: empty, text-only, #app-only, #2weeks, combined, #today/#yesterday,
        short forms (#3d/#month), unknown unit -> app, multiple durations widest
      * DB-backed: #edge filters by sourceApp; #2weeks filters by date;
        "invoice #edge" combines FTS text + app filter
  - New ClipFileStorageTests: 4 passed
      * MediaStore.storeFile copies bytes + preserves extension
      * saveCapturedFileClip persists kind=.file, filePath, mediaFilename, byteSize
      * reference-only path persists filePath with no local copy
      * duplicate file clip is deduped (count stays 1)
  - Full suite: 301 tests, 0 failures (285 prior + 16 new) -> migration v6 and
    model/search changes introduced no regressions.

VERIFICATION STATUS BY FEATURE
  F (search grammar):       VERIFIED (build + 12 tests incl. DB integration)
  C (file clips):           storage path VERIFIED (4 tests + full suite green, migration safe);
                            GUI capture/paste/move/extract NOT yet observed at runtime
  D (1-4 column grid):      build-verified; GUI runtime NOT yet observed
  E (date buckets):         build-verified; GUI runtime NOT yet observed
  A (streaming selection):  build-verified; runtime perf under heavy stream NOT yet observed
  B (freeze fix):           committed; ~48 KB heavy-stream runtime proof still pending

GUI RUNTIME: a smoke-launch of the rebuilt app was attempted but blocked by the
environment (app launch + defaults write denied). The clips panel (grid/date
buckets), file capture/paste/move/extract, and streaming-selection perf must be
confirmed by running the app manually. Steps are in the final report.
